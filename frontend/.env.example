# Public URL of the FastAPI backend (no trailing slash).
# Local dev: leave unset to default to http://localhost:8000
# Production: set this to your deployed backend URL.
VITE_API_URL=http://localhost:8000
