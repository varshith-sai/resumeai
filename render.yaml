services:
  - type: web
    name: resumeai-backend
    runtime: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    plan: free
    envVars:
      - key: CORS_ORIGINS
        sync: false
      - key: MAX_UPLOAD_BYTES
        value: "26214400"